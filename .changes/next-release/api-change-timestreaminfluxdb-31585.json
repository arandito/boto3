{
  "type": "api-change",
  "category": "``timestream-influxdb``",
  "description": "[``botocore``] Service-managed parameter groups now only apply optimized defaults to DB Clusters automatically. New field effectiveDbParameterGroupIdentifier surfaces the parameter group actually applied."
}
